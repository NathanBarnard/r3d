@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# If you ever switch to non-vendored deps, you can enable these lines:
# find_dependency(raylib CONFIG)
# find_dependency(assimp CONFIG)

include("${CMAKE_CURRENT_LIST_DIR}/r3dTargets.cmake")
